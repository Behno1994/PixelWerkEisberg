# Empfänger-Adresse für das Kontaktformular
CONTACT_RECIPIENT="info@pixelwerk.de"

# Optionaler Mail-Provider (z. B. Resend). Ohne Key wird die Anfrage nur geloggt.
RESEND_API_KEY=""

# Öffentliche Basis-URL (für Metadaten / OpenGraph)
NEXT_PUBLIC_SITE_URL="https://pixelwerk.de"
